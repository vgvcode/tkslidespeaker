Next, I ask a Code Generation tool (Amazon CodeWhisperer) to generate code in JavaScript for a problem.

Notice that once you describe what the function should do (in comments), it automatically creates the code in JavaScript. Of course, you should verify that this code is what you want.